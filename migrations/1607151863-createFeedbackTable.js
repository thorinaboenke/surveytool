exports.up = async (sql) => {
  await sql`CREATE TABLE IF NOT EXISTS feedbacks (
		feedback_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
		feedback_text VARCHAR(280),
		survey_id INTEGER NOT NULL REFERENCES surveys (survey_id) ON DELETE CASCADE ON UPDATE CASCADE);`;
};

exports.down = async (sql) => {
  await sql`DROP TABLE IF EXISTS feedbacks;`;
};
